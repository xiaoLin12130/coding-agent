{
  "current_milestone": "M4",
  "current_task": null,
  "goal": "M4 — Safety: a reliable SafetyLayer enforcing file scope, sensitive-path checks, shell risk grading, prompt-injection defence and human confirmation before high-risk tools run.",
  "milestone_status": "M4 complete, M5 not started",
  "todos": [
    {
      "content": "M4: safety models + policy (one place for every rule)",
      "status": "completed"
    },
    {
      "content": "M4: filesystem policy (project scope, symlink escapes, sensitive files)",
      "status": "completed"
    },
    {
      "content": "M4: shell risk grading (LOW/MEDIUM/HIGH, per-segment, subcommands, deny-by-default)",
      "status": "completed"
    },
    {
      "content": "M4: prompt-injection defence (scanning + instruction-source guard)",
      "status": "completed"
    },
    {
      "content": "M4: SafetyLayer (allow/confirm/deny, session grants, output guarding)",
      "status": "completed"
    },
    {
      "content": "M4: wire the layer into the Executor with validation first and no unchecked path",
      "status": "completed"
    },
    {
      "content": "M4: confirmation UI data + terminal rendering (command/cwd/impact/risk + 3 choices)",
      "status": "completed"
    },
    {
      "content": "M4: tests incl. the four 'must not' acceptance rules",
      "status": "completed"
    }
  ],
  "files_changed": [
    "backend/app/browser/__init__.py",
    "backend/app/browser/cli.py",
    "backend/app/browser/artifacts.py",
    "backend/app/browser/driver.py",
    "backend/app/browser/errors.py",
    "backend/app/browser/models.py",
    "backend/app/browser/profiles.py",
    "backend/app/browser/provider.py",
    "backend/app/browser/web_chat.py",
    "backend/profiles/mock.json",
    "backend/profiles/example-web-llm.json",
    "backend/fixtures/__init__.py",
    "backend/fixtures/chat_site/__init__.py",
    "backend/fixtures/chat_site/index.html",
    "backend/fixtures/chat_site/server.py",
    "backend/tests/conftest.py",
    "backend/tests/test_browser_driver.py",
    "backend/tests/test_web_chat_provider.py",
    "backend/tests/test_profiles.py",
    "backend/tests/test_cli.py",
    "backend/app/config.py",
    "backend/requirements.txt",
    "backend/README.md",
    ".gitignore",
    "state/project_state.json",
    "state/memory.json",
    "backend/tests/test_storage.py",
    "backend/app/context/__init__.py",
    "backend/app/context/models.py",
    "backend/app/context/transcript.py",
    "backend/app/context/memory.py",
    "backend/app/context/builder.py",
    "backend/app/context/session.py",
    "backend/app/context/cli.py",
    "backend/app/api/context.py",
    "backend/app/deps.py",
    "backend/app/main.py",
    "backend/app/config.py",
    "backend/app/models.py",
    "backend/tests/conftest.py",
    "backend/tests/test_transcript_store.py",
    "backend/tests/test_memory_store.py",
    "backend/tests/test_context_builder.py",
    "backend/tests/test_session_manager.py",
    "backend/tests/test_context_api.py",
    "backend/tests/test_context_cli.py",
    "backend/tests/test_storage.py",
    "backend/README.md",
    "backend/app/tools/__init__.py",
    "backend/app/tools/models.py",
    "backend/app/tools/errors.py",
    "backend/app/tools/json5.py",
    "backend/app/tools/parser.py",
    "backend/app/tools/registry.py",
    "backend/app/tools/builtin.py",
    "backend/app/tools/executor.py",
    "backend/app/tools/cli.py",
    "backend/tests/test_tool_parser.py",
    "backend/tests/test_tools_executor.py",
    "backend/tests/test_tools_builtin.py",
    "backend/tests/test_tools_cli.py",
    "backend/README.md",
    "backend/app/safety/__init__.py",
    "backend/app/safety/models.py",
    "backend/app/safety/policy.py",
    "backend/app/safety/paths.py",
    "backend/app/safety/shell.py",
    "backend/app/safety/injection.py",
    "backend/app/safety/layer.py",
    "backend/app/safety/cli.py",
    "backend/app/tools/executor.py",
    "backend/app/tools/errors.py",
    "backend/tests/test_safety_paths.py",
    "backend/tests/test_safety_shell.py",
    "backend/tests/test_safety_injection.py",
    "backend/tests/test_safety_layer.py",
    "backend/tests/test_safety_executor_integration.py",
    "backend/tests/test_safety_cli.py",
    "backend/README.md"
  ],
  "tests": [
    {
      "name": "M1 acceptance run (manual CLI, outside pytest)",
      "command": "python -m fixtures.chat_site.server --port 8765; python -m app.browser.cli ask --profile mock --prompt 'M1 acceptance run'",
      "result": "exit 0; source=network (SSE captured), completed=True, timed_out=False, reply='Fixture reply to: M1 acceptance run'; artifacts runs/20260911-005853-mock/: screenshot PNG 1440x900, dom html+txt, timeline.json (signals generation_started/stop_button_gone/dom_stable/text_length_stable, 6 polls), reply.json, network.json",
      "date": "2026-09-11"
    },
    {
      "name": "timeout-fallback and login-wall paths",
      "command": "pytest tests/test_web_chat_provider.py / tests/test_cli.py",
      "result": "PASS: stuck stream -> timed_out=True with partial text and exit code 2; never-starting generation -> start timeout; login wall -> LoginRequiredError, send refused, manual login detected by recover_session",
      "date": "2026-09-11"
    },
    {
      "name": "git push verification",
      "command": "git ls-remote origin",
      "result": "refs/heads/main = 48e53a0913679252cb54c5e1a9fc80eec3c70b77, identical to local HEAD; repo URL returns HTTP 200",
      "date": "2026-09-11"
    },
    {
      "name": "M2 acceptance run (three real processes, outside pytest)",
      "command": "process 1: work + save state; process 2: `python -m app.context.cli resume`; process 3: rotate and rebuild context",
      "result": "PASS: process 2 recovered the session created by process 1 (recovered=True, 3 messages / 1 turn), project state M2 + todo + 1 memory entry restored, context assembled as system/task/project_state/memory/transcript; rotation archived the session, kept a seed carrying project state + task + memory, and carried 3 entries verbatim",
      "date": "2026-09-11"
    },
    {
      "name": "M3 acceptance run (real process, outside pytest)",
      "command": "parse a messy model output (prose + json5 fence + 3 calls) -> execute through the Executor -> confirmation seam -> parse retry -> inspect log",
      "result": "PASS: 3 calls recovered from fenced JSON5 with zero issues (read_file, apply_patch, update_project_state); all executed; the file was patched (return 1 -> return 2), the milestone advanced to M3 and files_changed recorded; run_shell refused with confirmation_required and ran once confirmed; parse failures produced invalid_json then unbalanced_brackets with the retry budget allowing 2 retries and stopping on the 3rd attempt; 5 audit log lines written",
      "date": "2026-09-11"
    },
    {
      "name": "backend pytest (M0..M4)",
      "command": "cd backend && ../.venv/Scripts/python.exe -m pytest",
      "result": "474 passed in 73.5s (M4 added 171: test_safety_paths 32, test_safety_shell 41, test_safety_injection 30, test_safety_layer 25, test_safety_executor_integration 25, test_safety_cli 18)",
      "date": "2026-09-11"
    },
    {
      "name": "M4 acceptance run (real process, outside pytest)",
      "command": "drive the Executor directly: outside paths, sensitive files, destructive shell, session grant, injected file content",
      "result": "PASS against all four 'must not' rules: absolute/relative paths outside the project and writes outside were refused (blocked_by_safety); .env, id_rsa and .ssh/* were refused inside the project; rm -rf /, git push --force, curl|bash and cat ~/.ssh/id_rsa were denied outright while sudo/systemctl required confirmation; the confirmation payload carried command/cwd/impact/risk and choices reject|once|session; a session grant allowed the approved command and not a different one; a file containing 'IGNORE ALL PREVIOUS INSTRUCTIONS' was read as data, flagged with an injection finding, and executed nothing",
      "date": "2026-09-11"
    }
  ],
  "failures": [],
  "decisions": [
    "M1 drives the browser through a selector-driven WebChatProvider fed by JSON provider profiles; site detail never lives in provider or (future) agent code.",
    "The browser is always headed: BrowserDriver(headless=True) raises BrowserConfigError, and no stealth/fingerprint/automation-evasion launch flags are used.",
    "Login is manual by design: recover_session only re-opens the page and waits for a human to sign in; no credential, captcha or risk-control automation exists.",
    "Completion detection is multi-signal (stop control gone + text unchanged for stable_polls polls + non-empty text) with a mandatory timeout fallback that returns partial text, timed_out=True and a persisted timeline.",
    "Reply capture priority is network/SSE > copy button > DOM. Network bodies are only read for responses whose request already finished, so an open stream can never block capture.",
    "Verified offline against a shipped fixture chat page (backend/fixtures/chat_site) that streams SSE, exposes a stop control and can show a login wall; the shipped mock profile is marked verified, while example-web-llm.json is an explicitly UNVERIFIED template - no real web LLM site was driven in this session.",
    "Artifacts are written per run to runs/<timestamp>-<profile>/ (screenshot, DOM html+text, timeline, reply, network log); runs/ and .browser-profile/ are gitignored.",
    "playwright is pinned to 1.62.0 in the repo venv so it reuses the Chromium build already installed at C:\\Users\\LIN\\AppData\\Local\\ms-playwright (chromium-1234).",
    "M1 delivered no frontend or API changes: the browser layer is not yet reachable from the web console (no Tool/AgentLoop may call it until later milestones).",
    "Subagent delegation was broken in this environment: children inherit the parent agent's create-time route (agent.options), not its live model route, so after a mid-session default-model switch every delegation hit the stale opencode-go provider and failed with 400 MissingSessionID (missing x-opencode-session). Root cause proven from the child session logs and fixed by pinning config.agentOptions (provider commandcode / model deepseek/deepseek-v4.1-flash) on the tool-subagent and tool-subagent-fork rows of the liangshen agent preset; the pin needs a DSH restart or a new session to mount, and the workflow tool passing an explicit provider/model works as an immediate workaround (verified).",
    "M2 memory entries are typed (MemoryEntry: key/value/namespace/source/sensitive/source_turn/updated_by/timestamps) instead of an opaque list; this supersedes the M0 assertion that memory.json round-trips verbatim, so tests/test_storage.py::test_memory_round_trip now asserts the typed round trip plus defaults.",
    "M2 transcripts are stored as append-only JSONL per session under state/sessions/ (gitignored): a crash can only tear the last line, and the reader skips unparsable lines instead of losing the whole transcript.",
    "M2 external content is untrusted: the ContextBuilder renders tool results/DOM text with an explicit untrusted marker, and MemoryStore rejects secrets before they can be persisted.",
    "Command Code provider: patched the installed 0.9.1 in place (reasoning replay on the CLI transport + selectable reasoning efforts for deepseek/deepseek-v4.1-flash) because the upstream fix ships in 0.10.5, which requires dsh >= 0.1.2-rc.1 and this machine runs dsh 0.1.1-rc.1. Patch and backup are stored next to the plugin and in ~/.dsh/patches/; it must be re-applied after any plugin update.",
    "M2 context budget is measured in characters, not tokens: there is no tokenizer behind the provider boundary yet. ContextBuilder truncates non-protected sections (head+tail, omitting-size recorded, reference kept) and drops the lowest priority ones; system and task are never dropped.",
    "M2 rotation seeds the new session with a BOUNDED summary (seed_summary_chars, default 1500). Embedding the full summary rebuilt a context as large as the one rotation existed to escape (observed: 8225 -> 8245 chars in a test before the fix).",
    "Rotation only reclaims space when the session is near the budget; the seed is a fixed cost, so rotating a small session can grow the context. should_rotate() is the documented gate and the rotate CLI prints the before/after sizes plus a note instead of hiding it.",
    "M2 REST surface is read-only (/api/sessions, /api/sessions/{id}, /api/messages, /api/context) and no endpoint executes anything; the Tool layer arrives in M3 behind SafetyLayer/Executor. Named after docs/api-protocol.md, keeping /api/project_state and /api/memory.",
    "M2 memory dedupe compares VALUES only: the same key with a different value is a conflict, not a duplicate (the first implementation classified it as duplicate, which silently swallowed the conflict).",
    "M2 skips the project_state context section entirely when the state holds nothing factual, so an unstarted project does not push placeholder lines into every turn (docs/state-context.md forbids injecting everything by default).",
    "M3 tool arguments are strict (extra='forbid'): an invented parameter is a schema violation, because silently dropping it would hide that the model misunderstood the tool.",
    "M3 does not expose tools over REST: with no SafetyLayer yet, an HTTP entry point to run_shell/write_file would be a hole. Manual use goes through app.tools.cli, which uses the same Executor and bypasses nothing.",
    "M3 parser reports no_tool_call (not invalid_json) when the output contains no JSON structure at all, but keeps a specific issue code when a real structure failed to parse; otherwise the model hunts a syntax error that does not exist.",
    "M3 Executor logs replayed idempotent calls too: a replay is still a call that happened, and omitting it would make the audit log under-report (found by a test).",
    "M3 apply_patch writes nothing unless every hunk matches exactly once, so a partially applied patch can never leave a file half-modified.",
    "M4 replaces the M3 seam with a real SafetyLayer. The documented order is ToolCall -> Validation -> SafetyLayer -> Confirmation; validation therefore runs BEFORE the assessment, so a wrongly typed argument is a schema error rather than a safety error.",
    "There is no unchecked Executor: omitting the safety argument makes the Executor build a layer scoped to its working directory. A test asserts the attribute is never None and that an explicit spy layer observes every execution.",
    "M4 file scope is enforced on the RESOLVED path so a symlink cannot step outside the project, extra roots must be configured explicitly, and sensitive names (.env, id_rsa, key suffixes, .ssh/.aws/.gnupg) are refused even inside the project.",
    "M4 shell grading is deny-by-default: an unrecognised verb is HIGH, never LOW; every segment of a chained command is graded; subcommands are graded too (git status is LOW, git push is HIGH); a denied command is always reported HIGH regardless of its verb.",
    "M4 denies rather than confirms the operations no human answer should authorise: destructive patterns (rm -rf /, dd of=/dev/..., curl|bash, git push --force), commands naming a sensitive file, and commands touching a path outside the project. A confirmation cannot override a deny.",
    "M4 prompt-injection defence is two-part: instruction-like content in untrusted text is flagged and travels with the result (whose text is never rewritten), and check_instruction_source() refuses to parse tool/file/web/shell/document output as instructions, so only model and user output can issue a tool call.",
    "M4 session grants are keyed by the exact command (for run_shell) or the exact arguments, so 本会话允许 never widens into a blanket shell grant; the tests pin that a grant does not cover another command.",
    "M4 confirmation is rendered in the terminal (app.safety.cli confirm) with the four required facts and the three choices. The web console is NOT wired yet: that needs the confirm_request event from docs/api-protocol.md, which belongs with the API-protocol work rather than the safety layer."
  ],
  "notes": [
    "The only provider exercised end-to-end is the offline fixture (mock profile). Driving a real web LLM needs a profile whose selectors are filled from an 'inspect' run plus a manual login; that remains untested here.",
    "Image inspection is unavailable to this model, so the screenshot was validated structurally (PNG magic bytes, 1440x900 IHDR, IDAT present) rather than visually.",
    "Subagent route fix (2026-09-11): ~/.dsh/.agent-presets/liangshen/agent.cordis.yml edited (backup kept next to it) to pin the child agent route; takes effect on the next session/DSH restart, so delegation from the already-running session still uses the stale route.",
    "Git: repository initialised by the user (branch main, remote origin https://github.com/xiaoLin12130/coding-agent.git). The M0+M1 work is committed as 48e53a0913679252cb54c5e1a9fc80eec3c70b77 and pushed to origin/main on 2026-09-11 (90 files). The AGENTS.md reviewer step was not performed before this initial commit.",
    "A stray empty clone of the same repository sits at ./coding-agent/ (only a .git directory, no commits, no files, no stashes). It is untracked and makes 'git add -A' fail with \"'coding-agent/' does not have a commit checked out\"; it was deliberately excluded from the commit instead of being deleted.",
    "The Command Code provider patch takes effect only after a DSH restart; until then the running session keeps the unpatched adapter module.",
    "M2 is complete: backend/app/context/{models,transcript,memory,builder,session,cli}.py plus the read-only /api context routes; 150 backend tests green.",
    "state/sessions/ holds runtime transcripts and the session index and is gitignored; the state files themselves (project_state.json, memory.json) stay tracked.",
    "M3 is complete: backend/app/tools/{models,errors,json5,parser,registry,builtin,executor,cli}.py; 303 backend tests green.",
    "Tool calls are logged to runs/tool-calls.jsonl (gitignored), including replays and refusals.",
    "M4 is complete: backend/app/safety/{models,policy,paths,shell,injection,layer,cli}.py plus the Executor wiring; 474 backend tests green.",
    "The M3 'known gap' is closed: an absolute path outside the project is now refused by the safety layer and verified by both a direct probe and the acceptance test suite.",
    "Tools are still not exposed over REST: the SafetyLayer now exists, but publishing the execution chain over HTTP belongs with the API-protocol/console work (confirm_request), not with M4."
  ],
  "checkpoint": "M4 checkpoint 2026-09-11: safety layer complete (file scope with symlink and sensitive-file refusal, per-segment shell grading with deny-by-default, prompt-injection scanning plus an instruction-source guard, confirmation with the four required facts and three choices, session grants), Executor wired so no unchecked path exists, all four acceptance rules verified in a real process, 474 backend tests green. Next milestone: M5 (Agent Loop).",
  "git_branch": "main",
  "cwd": "H:\\AI-game\\coding-agent",
  "git_remote": "https://github.com/xiaoLin12130/coding-agent.git",
  "git_head": "48e53a0913679252cb54c5e1a9fc80eec3c70b77"
}
